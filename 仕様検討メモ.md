# 熱気球フライトゲーム 仕様検討メモ

国土地理院の地形データを読み込んで遊ぶゲームの仕様検討メモ。
仕様は確定済み。**プロトタイプ実装に着手済み**(`prototype/` フォルダ)。

## プロトタイプ現況(2026-07-08)

- 場所: `prototype/index.html` + `main.js` + `terrain.js`(ブラウザ+Three.js、ビルド不要)
- 起動: `python -m http.server 8000 --directory c:\Claude\game` → http://localhost:8000/prototype/
- 実装済み: 佐賀・嘉瀬川周辺20km四方の地形(DEM z13+写真z14)、バーナー(Space長押し)/リップライン(R長押し)、
  高度別風レイヤー(パイバルプリセット固定)、計器HUD、パイバル表、時間加速×1〜×8、視点切替(V)、燃料、出典表記
- 実装済み(JDG): マーカー投下(M、1本)。重力+空気抵抗(終端速度10m/s)で落下し落下中も風に流される。
  ターゲット(オレンジX+白リング、エリア中央)、着地点→ターゲットの距離をリザルト表示、
  自己ベストをlocalStorageに保存、HUDにターゲット距離/方位を常時表示
- 実装済み(競技の流れ): タスクブリーフィング画面(タスクシート+パイバル表+標準地図)。
  地図はズーム(ホイール、z11〜z17タイル切替)・パン(ドラッグ)対応のスリッピーマップ。
  地図クリックで離陸地点を自由選択(個別離陸)→離陸ボタンで飛行開始。
  制限時間30:00(ゲーム内時間)をHUDでカウントダウンし、時間内に投下できなければ現在地点計測にフォールバック
- 実装済み(風): ブリーフィングでパイバルを手入力可能(行の追加削除・プリセット4種・数値編集)。
  風テーブルは `?w=ft,dir,kt;…` 形式でURLに載り、「条件URLをコピー」ボタン(ブリーフィング/リザルト)で共有可能。
  風向補間は最短角度経路(350°→020°は北回り)に対応
- 実装済み(地形LOD): 気球の近く(3km以内)の地形タイルを裏でz16写真(約2m/px、従来の4倍精細)に
  段階アップグレード(1.5秒に1枚、上限12枚)。離陸地点とターゲットのタイルは離陸時に即時高解像度化。
  デバッグ用 `?autostart=1` でブリーフィングを飛ばして即離陸可能(自動テスト用)
- 実装済み(地形LOD第2段): 対地高度1000m未満のとき、気球直下の1タイルのみz17(約1m/px、4096px)へ
  さらにアップグレード。直下タイルが変わると前のタイルはz16に戻し、z17はメモリ上常に1枚だけ保持(実機確認済み)
- 実装済み(エリア選択): 起動時に日本全図のスリッピーマップでエリア中心をクリック選択
  (橙枠=20km四方プレビュー)。プリセット5件(佐賀・嘉瀬川/渡良瀬遊水地/佐久・千曲川/一関・平泉/上士幌)。
  エリアはURLの `?a=lon,lat` に載り、風(`&w=`)とセットで共有可能。`?a=`付きURLで開くと選択画面をスキップ。
  ターゲットは選択エリアの中央。海上など標高タイル欠損は標高0として処理(クラッシュしない)
  ※`?a=`直行→ブリーフィングの実機確認は未了(ヘッドレス不安定のため)。選択画面表示は確認済み
- 実装済み(一人称視点): ゴンドラ視点を作り直し。目の位置を気球に固定し、マウスドラッグで視線方向(ヨー・ピッチ、
  ピッチは±85°に制限)だけを回す方式に変更(従来は視線先の点を中心に軌道し目の位置がずれる挙動だった)。
  Vキーで視点切替時、直前の視線方向を引き継いでゴンドラ視点に入る/三人称視点に戻る際は見ていた向きの後方に回り込む
- 実装済み(ゴンドラ造形・2026-07-10): **四角い編みかご**構成で作り直し(PC版・モバイル版共通)。
  4面の壁(Canvas生成の編み目テクスチャ、内側からも見えるDoubleSide)+床+革張りリム(壁上面を覆う太さ)+
  四隅の革巻き風支柱+バーナー本体。一人称で見下ろすと「かごの中にいる」内観になることを
  スクリーンショット2アングル(-75°/-35°)で確認済み。三人称外観は実機確認待ち。
  検証用デバッグパラメータ `&pitch=-40&yaw=0`(fpv=1と併用、開始時の視線方向指定)を追加(モバイル版のみ)
- 実装済み(サウンド・2026-07-10): バーナー音(ノイズ600Hzバンドパス)・リップライン排気音(2600Hz)・
  風のアンビエント(風速連動、380Hzローパス)をWeb Audio合成で再実装。前回鳴らなかった原因は
  AudioContextがsuspendedのままresume()していなかったことと推定し、ユーザー操作(keydown/pointerdown)の
  たびにresumeする対処を実装。PC版・モバイル版共通。音量調整は updateSounds() 内の数値で可能
- 見送りのまま: バーナー使用中のカメラ微小シェイク、炎の毎フレームちらつき(ユーザー判断で不要)
- 実装済み(一人称の作り込み第2弾・2026-07-10、PC版・モバイル版共通):
  - 球皮内外にゴア(16枚の縦パネル)模様。内面は専用メッシュ(BackSide・自発光風の明るい配色)で
    「日光が透ける布」を表現。見上げるとゴアが放射状に集まる実機らしい見え方(スクリーンショット確認済み)
  - バーナー炎を二重コーン化(外オレンジ+芯黄、openEnded)し、基部をバーナー上端(y2.4)へ。
    点火中は球皮内面が滑らかに暖色へ明るくなる(ランダムなちらつきは不使用)
  - リップラインのロープ(赤、ゴンドラ→球皮の口)。R/排気ボタンを引いている間はロープが引き下がる
  - 一人称の立ち位置をゴンドラ中心から0.45m横へ(実機のパイロット位置。真上の炎が正しく見える)
  - マーカーに常時カメラを向く発光スプライト+リボン拡大。落下中はHUDに「落下中 ○○m」表示
  - デバッグ: `&burn=1`(autostart併用)で開始時からバーナー点火(モバイル版のみ)
- 実装済み(スカート・2026-07-10): 球皮の口からバーナー上方へ絞る開放円錐(上端r3.0/下端r1.5/高さ3.2、
  明るめの赤0xb52a2a。ユーザー指示で当初案より短く・明るく調整)。
  上端半径を球皮のy=7.0断面半径と一致させて接続し、継ぎ目の浮きを解消(過去2回の失敗原因)。
  三人称の気球らしいシルエットと、ゴンドラ視点の見上げ(内側もDoubleSideで表示)をスクリーンショット確認済み。
  PC版・モバイル版共通
- デバッグ用 `?fpv=1`(`?autostart=1`と併用)でゴンドラ視点のまま開始可能
- 実装済み(コンパス): 画面下部中央に円形コンパス。カメラ(画面正面)の磁方位を数値+回転文字盤で表示し、
  N(赤)/E/S/Wの方位文字と30°刻みの目盛り、ターゲット方向のオレンジ印付き。一人称・三人称どちらでも機能
- 実装済み(モバイル版改良・2026-07-10、`mobile/` のみ):
  - **風テーブルを対地(AGL)基準に変更**: 従来はMSL基準で、標高の高いエリアを選ぶと低高度レイヤーが
    地面より下になり使えなかった。`windAt(x, z, altM)` がその地点の地表標高を引いてAGLで風を引くように変更。
    気球・マーカーとも同じ扱い。HUDの大きな高度表示も対地ftに変更(パイバル表と同じ基準で風を読める)
  - **スマホ操作レイアウト刷新**: マーカー投下を左下から右側へ移し、右端の縦一列
    (下からバーナー/排気/マーカー投下)に集約。左半分は視点ドラッグ(左親指)専用に空けた。
    上部ツール(視点/加速/風データ)もスマホ幅では右上の縦一列にして計器パネルとの重なりを解消
  - **計器HUDのコンパクト化**: スマホ幅では既定で必須計器のみ(対地ft大表示・対地m・昇降・風・
    ターゲット・マーカー・残り時間+燃料バー)を表示し、タップで詳細(MSL・燃料%・温度・加速・状態)を開閉。
    背景も薄くして気球との重なりを軽減。ヘッドレス(390×844)でレイアウトと、
    時間加速×8での上昇→対地高度に応じた風の補間(190ft AGLで148°/4kt)を確認済み。実機確認は未了
- 実装済み(サウンドON/OFF・気球の影・2026-07-11、`mobile/` のみ):
  - **音 ON/OFFボタン**: 上部ツール列に追加。マスターゲインで一括ミュートし、設定はlocalStorageに保存。
    ※iPhoneでバーナー音が鳴らない報告あり。Web AudioはiOSのサイレントスイッチ(マナーモード)で
    消音される仕様のため、実機でスイッチ解除して要再確認
    → スイッチ解除でも鳴らないとの報告(2026-07-11)。対策第2弾を実装:
    (1) ユーザー操作中に無音の<audio>ループを再生してiOSのオーディオセッションを確立、
    (2) resume契機に touchend / click を追加(古いiOS対応)、
    (3) 音ボタンにオーディオ実状態を表示(「音 ON…」=コンテキストが動いていない)。
    実機で「音 ON…」のままなら resume 失敗、「音 ON」で無音なら出力経路の問題、と切り分けられる
    → **解決(2026-07-11実機確認)**。無音<audio>ループによるオーディオセッション確立が有効だった
  - **気球の擬似影**: 真下の地面に円形の暗がりを描画(シャドウマップ不使用で軽量)。
    対地高度が上がるほど大きく・薄くなり(0mで19m/不透明度0.55 → 高高度で最大60m/0.07)、
    対地の高さの目安になる。接地時に真下に出ることをスクリーンショット確認済み。
    ※三人称の既定カメラ角では対地約50mを超えると真下が画面外になる。上から見下ろす角度に
    ドラッグすれば見える。既定カメラをやや見下ろし気味に変えるかは要ユーザー判断
- 実装済み(リザルト演出・2026-07-12、PC版・モバイル版共通):
  - **着地後3秒のサスペンス**: 着地直後に結果が即表示され早すぎたため、HUDに「計測中...」を表示しつつ
    実時間3秒(setTimeout。時間加速の影響を受けない)待ってからリザルトを表示するように変更
  - **マーカー投下リプレイ**: 着地距離が10m未満の時だけリザルト画面に🎯リプレイボタンを表示。
    投下〜着地の位置を実時間タイムスタンプ付きで記録しておき、押すと三人称視点に切り替えて
    実際にかかった実時間で軌跡を再生(気球追従カメラと同じ差分平行移動方式)。再生中は
    シミュレーションを一時停止し、終了後はリザルト画面(距離・自己ベスト・応援ボックス等)を
    復元。ボタンは何度でも押し直して見返せる。ヘッドレスで一連の動作を確認済み
- 未実装(次の候補): 共通ローンチサイト指定のタスク

## ポーランド版(世界選手権2026・Krosno向け)

**→ プライベートリポジトリ `NGR2000/balloon-supporters` へ移管(2026-07-11)。**
世界選手権日本代表応援アプリとして、寄付者(登録メールアドレス)限定で公開する方針のため。
公開方式は Cloudflare Pages + Access(メールのワンタイムコード認証)。設定・運用手順は移管先のREADMEに記載。
今後のポーランド版・寄付者向け機能の開発は移管先リポジトリで行う。以下は移管までの検討記録。

- hitomiさんの仕様書 v0.2 をレビュー済み(2026-07-11)。方針=Copernicus DEM GLO-30(COG/S3)は妥当
- レビューでの修正点: GLO-30のファイル命名は `Copernicus_DSM_COG_10_...`(仕様書の`COG_30`はGLO-90の命名)。
  解像度30m=1 arc-second。緯度50°以上は経度方向の格子間隔が1.5倍(タイル幅2400px)になるため
  「全タイル同一幅」を仮定しない実装が必要(ポーランドはほぼ全土が該当。Krosnoは49.7°Nで手前)
- 開催地確定: 第26回FAI世界熱気球選手権 2026-09-20〜26 @ Krosno(約49.69N, 21.77E)。プリセット本命
- テクスチャ候補: EOX Sentinel-2 cloudless(OSM標準タイルは利用ポリシー上ゲームでの大量取得に不向き)
- CORS検証ページ `poland/cors-test.html` を作成(2026-07-11)。Copernicus DEMへのレンジリクエスト+
  EOXタイル3層+参考OSMをブラウザから直接fetchして✅/❌表示。開発環境は外部遮断のため実機で要実行
- **検証結果(2026-07-11、iPhone実機)**: Copernicus DEM S3 = ❌CORS拒否 /
  EOX Sentinel-2 cloudless(2024/2022/2020)= ✅全てOK(テクスチャ確保)/ OSM = ✅
- DEM代替の本命: **AWS Terrain Tiles(terrarium形式)** `s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png`。
  地理院標高タイルと同じXYZ PNG方式(デコード式のみ違う: h = R*256+G+B/256-32768)で、
  terrain.js をほぼ流用可・geotiff.js不要
- **検証結果(2026-07-11、iPhone実機)**: terrarium ✅(Krosnoタイル中央の標高277.5m=実際の街の標高と一致)。
  **データ構成確定: 標高=terrarium / テクスチャ=EOX Sentinel-2 cloudless 2024 / 完全サーバーレス**
- **`poland/` 実装(2026-07-11)**: mobile/ をベースに作成(index.html + main.js + terrain.js)
  - terrain.js: DEM=terrarium z13(海域の負値は0にクランプ)、テクスチャ=EOX z14 2x2合成。
    EOXは素材10m解像度のためz16/z17段階アップグレードは無し(APIは互換のため空実装で残す)
  - main.js: プリセット5件(クロスノ=世界選手権2026/レシュノ/ナウェンチュフ/エウク/ノヴィ・タルク)、
    エリア範囲チェックをポーランド(lon 14〜24.2, lat 48.9〜55)に、選択/ブリーフィング地図をOSMタイルに変更、
    エリア枠を緯度依存の実サイズ(z13×5枚、緯度50°で約16km)で描画、自己ベストkeyを日本版と分離
  - index.html: タイトル・出典表記(EOX/Terrain Tiles/OSM)を変更
  - ヘッドレス検証済み(ダミータイル280m差し込み): MSL=地形280m+AGL、風の対地参照、
    プリセット表示、エラー無し。実機(実タイル)での見た目確認は未了
  - 注: Copernicus DEM(DSM・30m)ではなくSRTM系30m(terrarium)になったため、仕様書2章の
    「DSM由来の扱い」の論点は解消(terrariumは地表モデル寄り)。緯度50°問題も不発生(XYZタイルのため)
- **高解像度化(2026-07-11)**: 気球の近く3km以内のタイルを Esri World Imagery の z16(≒2.4m/px)へ
  段階アップグレード+直下1タイルをz17化する日本版のLOD機構を復活(ソースをEsriに差し替え)。
  ヘッドレスで近景タイルの差し替え適用を確認済み。出典表記に Esri, Maxar, Earthstar Geographics を追加。
  標高は元データが30m級のため現状が上限(メッシュ約25m間隔で既に使い切り)
- さらに上の候補: ポーランド公式Geoportalオルソフォト(25cm級・国のオープンデータ)。
  WMTS(EPSG:3857)のCORS可否を cors-test.html に追加済み、実機結果待ち。✅ならEsriから差し替え検討
- **実機検証結果(2026-07-11)**: Esri z17 = ✅(ゲーム内でもKrosno飛行場の滑走路・建物まで視認できることを
  実機スクリーンショットで確認)。Geoportalオルソフォト = 応答が返らず「実行中」のまま(タイムアウト追加済み)。
  → **近景はEsri採用で確定**。Geoportalは将来URL形式を再調査する場合のみ再検討
- **ポーランド版のデータ構成(最終)**: 標高=AWS Terrain Tiles terrarium z13 /
  遠景=EOX Sentinel-2 cloudless 2024 z14 / 近景=Esri World Imagery z16・直下z17 /
  2D地図=OSM / 完全サーバーレス・実機動作確認済み
- 構成方針(提案): poland/ を丸ごと複製せず、terrain.js をデータソース差し替え可能にして本体を共用する

## モバイル版(`mobile/` フォルダ、2026-07-09作成)

PC版(`prototype/`)は完成として凍結し、スマホ対応版を `mobile/` に分離。PC版からのコピーに以下を追加:

- **タッチ操作ボタン**: バーナー(右下・大、押している間噴射)/排気(その上)/マーカー投下(左下)。
  離陸後に表示。ポインタイベントで長押し対応、コンテキストメニュー抑止
- **上部ツールボタン**(右上): 視点切替/時間加速(タップで×1→×2→×4→×8巡回)/風データ表(開閉)
- **タッチジェスチャー**: ゴンドラ視点のルック操作、エリア選択地図・離陸地点地図のドラッグパン+
  タップ選択+2本指ピンチズームをポインタイベントで実装(マウスと共通コード)。三人称はOrbitControlsのタッチ機能
- **モバイルレイアウト**(max-width 760px): 計器縮小、パイバル表・キーヘルプは非表示(風データボタンで開閉)、
  コンパス縮小、ブラウザ既定のスクロール/ダブルタップズーム無効化(touch-action: none, user-scalable=no)
- キーボード操作もそのまま併用可(PCブラウザでも動く)
- 検証: 幅500pxのヘッドレスで全UI配置を確認済み。幅414px以下で右側ボタンが切れて見えるのは
  ヘッドレスの最小ウィンドウ幅(約480px)由来のアーティファクトで、実機スマホでは正しく配置される見込み。
  実機(タッチ)での操作確認は未了

## 地上風のゆらぎ(β)機能の追加(`prototype/` + `mobile/`、2026-07-19)

**きっかけ**: 事前にパイバル表を読めるだけだと安心して飛べてしまい、「現実の地上付近の風は
変わりやすい」という駆け引きが再現できていなかった。実競技でも高気圧との位置関係次第で
地上風は上空より乱れやすく予測しづらくなる、という気象の性質をゲームに取り込みたい。

- **段階的な導入方針**: 天気図を画像として直接解析するのは現状の実装では難しいため、まずは
  「高気圧までの距離(km)」という単純な数値をプレイヤー自身がブリーフィングで入力する形にした。
  距離が近いほど地上風が表の値からズレやすい、という関係だけを簡易的に再現する第1段階。
  将来的には天気図の自動読み取りに置き換えていく想定(今回は見送り)
- **隠しパラメータの設計**:
  - 距離を3段階のレンジ(150km未満/150〜400km/400km以上)に分け、レンジごとに
    「風向のズレ幅」「風速の残存率」の範囲を設定(距離が近いほど広い/小さい)
  - 実際のズレ量(風向オフセット・速度残存率)と、時間変化の周期・位相は離陸時に1回だけ
    乱数で決まり、パイロットには数値として見せない(距離だけは公開情報として表示)
  - ズレは地上0〜500ft AGLの帯にのみ適用し、線形にブレンドして上空の表通りの値へ戻す
  - さらに約4〜6分周期・振幅±7.5°(風向)/±30%(風速)のゆらぎを加え、同じ「地上クルー」への
    確認でも時間帯によって値が変わるようにした(周期・位相も離陸時に乱数で決定)
  - **控えめな値に留める方針**: 実装当初からズレ幅を広げる案もあったが、「まずはずれは控えめで
    いい。後々、みんなに慣れてもらってから現実に近づけていきたい」という判断で現在の範囲に確定
- **地上クルーという情報源**: 実際の気球競技でもターゲット班(地上クルー)による地上風の報告が
  最終的な狙いの精度を左右する重要な情報という実態を再現。飛行中に呼び出すと、その瞬間の
  「本当の地上風」を報告する
  - 方向はTO表示(進行方向)で精密な数値を返す
  - 速度は地上クルーが目視でしか把握できない想定のため、「弱い風/普通の風/強い風」の
    3段階でしか報告しない(数値化しない)
  - 呼び出し手段: `prototype/`(PC版・キーボード操作)は`G`キー、`mobile/`はタッチボタン
    「地上クルー」。呼び出すたびに更新し、6秒ほどで表示が消える(情報の鮮度を表現)
- **風データパネルの地上行**: ゆらぎON時、パネルの0ft行だけ離陸時点(t=0)の値でグレーアウト
  表示する。これは地上クルーの報告とは別に「パイロット自身が離陸時に読んだ値のまま古びていく」
  ことを示す演出で、地上クルーへの確認が持つ価値(鮮度の高い情報)との対比になっている
- **視覚的な判別手段**: 動画などで後から見返した時にも有効/無効がひと目で分かるよう、
  ターゲット(X+リング)の色を無効=暖色系(オレンジ×黄)/有効=寒色系(青×緑)に切り替え、
  結果画面にも小さく有効/無効を表示するようにした
- **マルチプレイ版(`balloon-supporters`リポジトリの`poland/`)への展開**: 同じ機能を
  マルチプレイでも成立させるため、隠しパラメータはホストが1回だけ決めて条件配布メッセージに
  乗せ、全プレイヤーで共有する設計にした(サーバーは中身を解釈せずそのまま中継・永続化するだけ)
- **スコープ**: 本家(`prototype/`・`mobile/`)にはマルチプレイが無いため、上記のうち
  マルチプレイ同期の設計は`balloon-supporters`側のみの対応
- **実機フィードバックによる追加修正**:
  - iOSで長押しした際にコピー/検索の選択メニューが出てしまう不具合を発見。ゲーム画面全体に
    `-webkit-touch-callout: none`等を指定して抑止(`poland/`で先に見つかり修正した不具合と同根)
  - タッチ操作ボタン列に「地上クルー」が増えたことで縦に伸び、狭い画面の端末では
    マーカー投下ボタンと重なる不具合が発生。マーカー投下ボタンを排気の直上まで下げ、
    誤操作時の影響が小さい「音ON」を投下ボタンに近い最下段に、「地上クルー」をひとつ上に
    入れ替えてヒューマンエラーを回避。ボタン間隔は実機フィードバックを受けて数回微調整した

## マルチプレイの公開版展開(`hot-air-balloon1`、2026-07-19)

**きっかけ**: マルチプレイ(みんなで飛ぶ)機能はこれまで寄付者版(`balloon-supporters`リポジトリの
`poland/`)にのみ実装されていた。これを公開版(このリポジトリ)へも展開する準備を開始。

- **展開範囲**: `mobile/`・`prototype/`(PC版)の両方に展開することを決定
  (寄付者版は`poland/`=モバイル操作前提の1系統のみだったが、公開版は既存の2系統それぞれに統合)
- **サーバーの扱い**: 寄付者版のマルチプレイサーバー(Cloudflare Workers + Durable Objects)を
  共用せず、**公開版専用に新しくサーバーを立てる**方針に決定。理由は
  (1) 公開版のアクセス負荷が寄付者版サーバーに影響しないようにするため、
  (2) ルームコードの名前空間を分離するため。
  `server/`ディレクトリへ寄付者版のサーバー実装をそのまま移植し、`wrangler.toml`の
  worker名だけ`balloon-multiplayer-public`に変更(`name`が違えば同一Cloudflareアカウントでも
  別Worker・別ルームコード空間になる)
- **クライアント統合**: `mobile/net.js`・`prototype/net.js`(内容は同一、`DEFAULT_SERVER`のみ
  参照先)を新設し、`mobile/main.js`→`prototype/main.js`の順に統合。`prototype/`は元々i18nが
  無くキーボード・マウス操作前提のため、多言語文字列を持たせず、`windAt()`の引数もPC版の
  仕様(高度のみ・MSL基準)に合わせて実装した(`mobile/`・`poland/`はAGL基準+xz座標を取る3引数版)
- **検証方法**: 開発環境ではThree.jsのCDN読み込みがネットワークポリシーと`file://`のCORS両方で
  ブロックされ、`main.js`がブラウザ内で実行できない。そのため
  (1) 構文チェック、(2) i18nキー/DOM要素IDの相互参照を検証するNodeスクリプト、
  (3) Playwrightで`file://`のHTMLを直接操作してのレイアウト確認、
  (4) `net.js`(THREE.js非依存)を直接Nodeからimportし、実際にローカル起動した`wrangler dev`
  サーバーへ本物のWebSocketプロトコルで通す形でのE2E確認、の4段階で代替検証を行った

### 公開版の同時飛行4機制限(2026-07-19)

**きっかけ**: 公開版は不特定多数がアクセスしうるため、サーバー負荷を抑える目的で
「同時に飛べるのは1ルームあたり4機まで」という制約を導入したいという要望。
将来的にサーバーを増強できた段階で、この上限は緩和する想定(暫定措置)。

- `wrangler.toml`の`MAX_PLAYERS`を16→4に変更し、コメントで「公開版は4機まで同時フライト
  (サーバー負荷低減のため。将来増強予定)」という経緯を明記
- **満室+全員準備完了で自動開始**: 4機目が入室して全員が準備完了(離陸地点選択済み)になった
  時点で、ホストが「開始」ボタンを押さなくても自動的にカウントダウンへ入る仕様に決定。
  (検討時の論点: 単に「4人目が入室した瞬間」に準備未了でも強制開始する案もあったが、
  離陸地点未選択のまま飛行開始してしまうため不採用。「全員準備完了」も条件に含める案を採用)
- 実装は`server/src/room-core.js`の`start()`から遷移処理を`beginCountdown()`として切り出し、
  `setReady()`側からも(満室+全員ready条件を満たした場合に)同じ処理を呼べるようにした。
  ホストによる手動開始(2〜3人・全員ready時)は従来通り併存させている
- Node単体テスト2件追加(自動開始・準備未了時は開始しないことの確認)、加えて
  `wrangler dev`実サーバーへの4人E2E確認(5人目が満員で拒否されること、3/4では未開始、
  4/4で自動的にcountdownが届くこと)も実施

### 公開版サーバーのCloudflareアカウント分離(2026-07-19)

**きっかけ**: 公開版サーバーのデプロイ先を寄付者版と同じCloudflareアカウント
(workers.devサブドメインが`rnogamigm`=個人を示す文字列)に間借りする形で進めていたが、
公開版のURL(`balloon-multiplayer-public.rnogamigm.workers.dev`)に個人名が写り込んでしまう
ことが判明。これを解消するため、公開版専用の新しいCloudflareアカウント
(workers.devサブドメイン`hotairballoongames2026`)を作成し、そちらへ再デプロイする方針に決定

- このセッション(サンドボックス環境)はネットワークポリシー上`api.cloudflare.com`へ
  直接アクセスできないため、`wrangler deploy`はユーザーの手元PCで実行してもらう運用にした
- **つまずいた点1**: 初回、APIトークンを発行せずに`wrangler deploy`を実行したところ、
  PCに残っていた旧アカウント(rnogamigm)のログインキャッシュが優先され、
  意図せず旧アカウント側に同名Workerが複製される形になった
- **つまずいた点2**: 新アカウント用のAPIトークンを`CLOUDFLARE_API_TOKEN`に設定して
  再実行しても認証エラー(`Authentication error [code: 10000]`)。エラーメッセージ中の
  アカウントIDを確認したところ、リクエスト先が旧アカウントのIDのままで、
  トークンが紐づく新アカウントのIDと不一致だったことが原因と判明
  (`wrangler.toml`に`account_id`の指定が無いため、PC側に残っていた古いデフォルト
  アカウントの解決結果を使おうとしていたと推測)
- **解決**: `CLOUDFLARE_ACCOUNT_ID`環境変数に新アカウントのIDを明示指定してから
  `wrangler deploy`を再実行し、`https://balloon-multiplayer-public.hotairballoongames2026.workers.dev`
  へのデプロイに成功。`mobile/net.js`・`prototype/net.js`の`DEFAULT_SERVER`をこのURLに更新
- 後片付け: 旧アカウント側に誤って作られた重複Workerの削除、および作業用に発行した
  APIトークンの失効(いずれもユーザー側の対応が必要な作業として案内)

## 多言語対応の拡張: フランス語・ドイツ語・スペイン語を追加(2026-08-04)

### きっかけ

Instagramのアクセス解析でフランス語圏からのアクセスが多いことが判明。これを機に、
単発でフランス語だけ追加するのではなく「気球競技が盛んな主要国」も踏まえて拡大方針を
検討した。

### 言語選定

気球競技の実際の大会規模を基準に検討:

- **フランス語**: Instagramの実データに加え、世界最大の熱気球イベント
  「Lorraine Mondial Air Balloons」(フランス・シャンブレー、隔年開催、1000機超規模)の
  開催国
- **ドイツ語**: ドイツ・オーストリア・スイスの3か国をまとめてカバーできる。
  Gordon Bennett Cup(長距離ガス気球競技)の常連国が集中
- **スペイン語**: 世界的な熱気球メーカー**ULTRAMAGIC社**(スペイン・ビック)の本国。
  European Balloon Festival(Igualada、スペイン)の開催国でもあり、話者人口も多い
- オランダ語・イタリア語は気球文化はあるが規模が上記3言語より一段小さいため見送り

### 実装

既存のpt/pl追加時と同じパターンで、`mobile/i18n.js`・`prototype/i18n.js`(本リポジトリ)、
`poland/i18n.js`(`balloon-supporters`リポジトリ)の`STR`オブジェクトに`fr`/`de`/`es`
ブロックを追加し、`SUPPORTED_LANGS`にも追加。各`index.html`の`#lang-select`に
`<option>`を追加。キー数は`ja`ブロックと完全一致することを一時的なNodeスクリプトで
検証(mobile 154キー、prototype 142キー、poland 218キー、いずれもfr/de/es全て一致)。
`data-i18n`系属性がHTML側で参照するキーが辞書に存在することも確認済み。

### 検証

`node --check`(3ファイルとも)、`server/`の既存テスト(28件)、`poland/`の既存テスト
(140件)はいずれもpass。翻訳はネイティブチェックを経ていないため、公開前に
フランス語・ドイツ語・スペイン語ネイティブ(または信頼できる翻訳者)による文言レビューを
ユーザー側で実施することを推奨する。

## 気球の色・柄カスタマイズ機能を移植(`balloon-supporters`/`poland/`から、2026-08-09)

### 背景

`balloon-supporters`リポジトリ(ポーランド世界選手権2026・Krosno向け特別仕様、
`poland/`クライアント)で先に開発・検証済みだった「気球の色・柄を選べる」機能一式を、
本家公開版(本リポジトリ、`prototype/`+`mobile/`クライアント+独自のマルチプレイ
サーバー)にも反映してほしいとの依頼で移植した。両リポジトリは似た構造だが
独立に保守されているコードベースのため、機械的なコピーではなく各クライアントの
既存構造(このリポジトリはタスクチェーン等の機能を持たないシンプルな1タスク制で、
`prototype/`と`mobile/`は`net.js`を含む複数ファイルをバイト同一に保つ慣習がある)
に合わせて移植した。

### 移植した内容

- **`balloon-appearance.js`**(新規、`prototype/`・`mobile/`ともバイト同一):
  DOM/three.js非依存の純粋ロジックモジュール。パターン(交互/水平/スパイラル/
  サファイア)・色数(1〜4色)・単色時の塗り潰し方式・ロードテープ色(茶/白/透明)を
  16進数コード(最大6桁)で符号化・復号化する。サファイアパターンは実機の写真を
  参考に調整済みの、3回対称の山形境界+市松模様を組み合わせた柄
  (`poland/balloon-appearance.js`と完全に同じ実装を流用)。
- **サーバー(`server/`)**: `RoomCore.join()`に`appearance`引数を追加し、16進数
  6桁以内の文字列として検証・保存(不正値/省略時は`null`)。`roster()`・
  `resultsRows()`で他クライアントへ中継。サーバー自身は中身を解釈しない不透明な
  文字列として扱う。`RoomDO.fetch()`でURLクエリの`appearance`をそのまま
  `RoomCore.join()`へ橋渡し。
- **`net.js`の`Room.connect()`**: `appearance`パラメータを追加し、
  `&appearance=...`としてWebSocket URLへ含める。
- **`index.html`(両クライアント)**: 従来の色スウォッチのみのUI
  (`#mp-colors`/`#mp-join-colors`)を、パターン・色数・色スウォッチ×4・
  ロードテープ・16進コード入力・3D回転プレビューを持つ`#balloon-modal`モーダルに
  置き換えた。ソロプレイのブリーフィング画面にも新たに「気球の色・柄」選択ボタンを
  追加(従来このリポジトリのソロプレイには気球の見た目選択機能自体が無かった)。
- **`main.js`(両クライアント)**: `buildGoreTexture()`をパターン対応の実装に
  差し替え、`skirtColorFor()`(サファイアのように開口部の円筒を最上段色に
  合わせるパターン用)を追加。モーダルの開閉・描画・適用ロジック、専用の
  Three.jsシーンによる3D回転プレビュー、`localStorage`(キー`balloon-appearance`。
  `prototype/`と`mobile/`は同一オリジンなので共有され、どちらで選んでも
  引き継がれる)への永続化、マルチプレイでの外見共有
  (`appearanceOfPlayer()`→`buildGhostBalloon()`でゴースト気球にも反映)を実装。
  招待URL・再戦時の自動再入室は、旧`c=`(色番号)パラメータを`ac=`(外見コード)に
  置き換えた。
- **i18n**: `balloon.*`名前空間(セクション見出し・モーダル文言・パターン名・
  ロードテープ名など22キー)を、既存の7言語(ja/en/pt/pl/fr/de/es)すべてに
  `poland/i18n.js`の訳文をそのまま流用して追加。

### 検証

- `node --check`: 変更・新規作成した全ファイル(`prototype/main.js`・`net.js`・
  `i18n.js`・`balloon-appearance.js`、`mobile/`側も同様、`server/src/room-core.js`・
  `room-do.js`)がいずれもpass。
- `node --test`: `server/test/*.test.mjs`(29件、appearance検証・中継の新規テスト
  1件を含む)、`prototype/test/balloon-appearance.test.mjs`(`poland/`の
  テストスイートをそのまま移植、21件)、合計50件すべてpass。
- 実ブラウザ検証: `node_modules/three`をローカルにvendorし`index.html`の
  importmapを差し替えたコピーをPlaywrightで操作。`prototype/`・`mobile/`
  両方で、ブリーフィング画面→「気球の色を選ぶ」ボタン→モーダルが開く→
  サファイアパターン・3色を選択→3D回転プレビューに正しく反映→コード生成→
  「決定」でボタンの代表色ドットが更新、の一連の流れをコンソールエラー無しで確認。
  マルチプレイでの2クライアント間の外見共有は、`poland/`側で同等の仕組みを
  既に実地検証済み(このリポジトリのサーバー実装も同じ`RoomCore`ベースの
  ロジックを踏襲しているため、コードレベルでの等価性を確認する形で代替した)。

## 気球の柄を「4パターン固定」から「数式(パラメータ)+プリセット」へ(2026-08-11)

### きっかけ

柄の追加を検討したところ、共有コードの箱がすでに満杯で1bitしか空いていないことが判明した。
現行フォーマット(16進6桁=24bit)の内訳は パターン番号2bit(4種で満杯)/色数-1が2bit
(4色で満杯)/単色ベタ塗り1bit/色4個×4bit/ロードテープ2bit で、**5種類目の柄も
5色目も入らない**状態だった。一方で単色の気球でも常に4色分16bitを払っており、
使っていない容量を払わされてもいた。

やりたいことは「実機の平面図(展開図)を見ながらパラメータを合わせれば、その柄が出せる」形。
ただし何でもパラメータ化すると桁数が肥大化するので、**自由度と容量のバランス**を設計の軸にした。

### 設計の原則

**全プレイヤーで共通のものはクライアントのコードに置き、共有コードには
「プレイヤーごとに違うもの」だけを載せる。**

柄の定義そのもの(サファイアの上下位相ずらしのような手調整済みロジック)は全員の
クライアントが持っているので、共有コードには「何番の柄か」だけあればよい。逆に自作の柄は
他人のクライアントが知らないのでパラメータを全部載せる必要がある。この線引きがそのまま
下の二階建てになっている。

### 決めたこと

- **コードを可変長にした**。`encodeAppearance()`は「現行の4パターン・4色以内で
  表せる見た目ならv0(16進6桁)のまま出す」ので、**既存の共有URL・localStorageの値は
  1文字も変わらない**。桁が増えるのは新しい柄や5色目を実際に使ったときだけ
- **文字あたりのビット数を上げた**。16進(1文字4bit)ではなくCrockford Base32
  (1文字5bit、I/L/O/Uを除く32文字)にしたので同じ情報量なら約25%短い。大文字のみで
  I/L→1・O→0と読み替えるため、口頭で伝える用途でも16進と同程度に扱える
- **柄を二階建てにした**
  - **プリセット階(5bit)**: 番号だけ。0〜3が現行の4パターンで、残り28枠に実機由来の
    手調整済みの柄を足していく。サファイアのように「上から6段だけ位相を4マスずらす」
    「最下段はベタ塗り」といった写真合わせの例外を含む柄は数式化せずここに置く
  - **カーネル階(20bit)**: パラメータ式。現行4パターンから共通形を取り出した
    `band = floor(Kv・v + Kg・(g/GORES)) + 波(slice)` を、横分割数S(3bit)/縦ステップ
    Kv(5bit)/横ステップKg(5bit、符号=巻き方向)/波の振幅(2bit)/波の周期(2bit)/
    市松(1bit)/色の割当方式(2bit: 巡回・折り返し・グラデ・グラデ+市松)で表す
- **色は使った数だけ払う**。色数フィールドを2bit(最大4)→4bit(最大16)にし、色本体は
  実際に使うぶんだけ書く。パレットも12→16色に増やしたが、**1色=4bitという枠は元から
  同じなので容量は増えていない**(追加は 藍/山吹/ラベンダー/ミント)

### 実際の桁数

| 内容 | 文字数 |
|---|---|
| 現行4パターン・4色以内 | **6**(今までと同一) |
| プリセット柄・8色 | 10 |
| カスタム柄・4色 | 10 |
| カスタム柄・8色 | 13 |
| カスタム柄・16色 | 20(最長) |

固定長で16色ぶんの箱を常に持つと全員が20桁を払うことになるが、この方式なら定番の気球は
今まで通り6桁で済み、凝った人だけが長いコードを持つ。

### カーネルが現行パターンをどこまで再現するか

- **交互・水平は完全一致**(テストで全マス比較して確認済み)。交互=`Kv 0, Kg 16`、
  水平=`Kv 10, Kg 0`
- **スパイラルはほぼ同型だが完全一致ではない**。現行は球の正投影から導いた
  `SPIRAL_TOTAL ≈ 9.02` を使うが、Kvは整数5bitなので9に丸まる。見た目はほぼ同じだが
  厳密には別物なので、スパイラルはプリセット階に残したままにしてある
- **サファイアは数式化しない**。上下の位相ずらし・最下段のベタ塗り・zone境界の
  二重波防止といった写真合わせの例外があり、数式に載せると見た目が劣化するため

### 検証

- **回帰(最重要)**: 変更前の実装で4パターン×色数1〜4の全マス(16分割・24分割の両走査)を
  ハッシュ化しておき、変更後と**完全一致**することをテストで固定した。サファイアの
  手調整が1マスでも変われば落ちる
- **v0互換**: 4パターン×3テープ×塗り潰し2種×色の組み合わせ 計86,688通りの出力コード
  文字列が変更前と**バイト単位で同一**であることを確認
- `node --test`: `prototype/test/balloon-appearance.test.mjs` 35件(既存21件+新規14件)、
  `server/test/*.test.mjs` 31件(既存29件+新規2件)、いずれもpass
- **ブラウザ**: `three`をローカルにvendorしてimportmapを差し替えたコピーをPlaywrightで操作。
  `prototype/`・`mobile/`両方で「柄にカスタムが出る→パラメータ欄が開く→8色に増やす→
  1スロット16色から選べる→3Dプレビューに反映→13文字のコード生成→決定→開き直して復元→
  壊れたコードに警告→貼り直して一致」までをコンソールエラー無しで確認
- **サーバー**: `wrangler dev`に2クライアントを繋ぎ、20文字のカスタムコードと従来の
  16進6桁が**混在しても双方そのまま中継される**ことと、パレット16色目(番号15)が
  通ることを確認。サーバーは中身を解釈しない不透明な文字列として扱う方針は変えず、
  検証を `/^[0-9A-Za-z]{1,24}$/` に緩めただけ

### 複数機フライト時の帯域・描画への影響(2026-08-11 実測)

「柄を使った機体が増えたときに重くならないか」を実測した。

#### 帯域: 実質ゼロ

**appearanceは`snap`(位置スナップショット)に入っていない**。`roster`(入退室・準備完了・投下など
局面が変わったときだけ)にしか載らないので、飛行中ずっと流れ続けるストリームには一切影響しない。

`wrangler dev`に8機を接続し、実際に離陸〜飛行させて受信バイトを種類別に数えた結果:

| メッセージ | 頻度 | 現行6桁コード | 新形式20桁コード |
|---|---|---|---|
| `snap` | 4Hz(常時) | 289 B/通 | **291 B/通(実質同じ)** |
| `roster` | 局面が変わったときだけ | 861 B/通 | 950 B/通(+89 B) |

8人ぶんで`roster`1通が+89 B、つまり**1人あたり+11 B**。30分のフライトなら`snap`が
7,200通で約2.1 MBに対し、`roster`側の増分は数KBにとどまり、**全体の0.2%程度**。
サーバー側はコードの中身を解釈しない不透明な文字列として中継するだけなので、CPU影響も無い。

#### 描画: 柄の種類・色数によらず一定

three.jsの計測値。**見た目を変えても描画資源はまったく変わらない**:

| 機体数 | drawCall | 三角形 | テクスチャ |
|---|---|---|---|
| 1機 | 4 | 390 | 2 |
| 8機 | 32 | 3,120 | 16 |
| 32機 | 128 | 12,480 | 64 |

1機=球皮・スカート・ゴンドラ・名前ラベルの4描画で固定。柄は512×64のテクスチャ1枚に
焼き込まれるので、16色でも48分割でも**テクスチャの枚数もサイズも増えない**。
8機でテクスチャ約1.6 MB。

#### 唯一のコスト: 入室時のテクスチャ生成(1回だけ)

柄によって変わるのは`buildGoreTexture()`のCPU時間だけ。機体ごとに入室時1回。

| 柄 | 1枚の生成時間 |
|---|---|
| 単色 | 0.18 ms |
| 交互4色 | 0.43 ms |
| サファイア4色(24分割) | 0.71 ms |
| カスタム8色(24分割) | 0.68 ms |
| カスタム16色(48分割・最重) | 3.51 ms |

8機が一斉に入室しても合計11〜17 ms。60fpsの1フレーム(16.7 ms)相当で、しかもロビーでの
出来事なので飛行中のカクつきにはならない。

#### 見つかった性能バグと修正

調査の過程で、**カスタム柄だけ1呼び出しあたり508 nsとプリセット(57 ns)の約10倍遅い**ことが
判明した。原因は`resolvePattern()`が呼び出しのたびに`JSON.stringify(clampKernel(...))`で
キャッシュキーを作っていたこと。`buildGoreTexture()`は1枚あたりcolorSlotAt()を千回以上
呼ぶので、その全てで文字列の確保が走っていた。

キャッシュ判定を7フィールドの直接比較に変え、**508 ns → 136 ns(3.7倍速)**に改善。
テクスチャ生成も3.5 ms → 0.68 ms(カスタム8色)へ。生の値で覚えるので、呼び出し側が
カーネルを書き換えた場合もきちんと作り直しになる。

#### 結論

柄付きの機体が増えても、**帯域・描画とも実用上の影響は無い**。増えるのは入室時に1機あたり
0.2〜3.5 msのCPU時間と、`roster`1通あたり1人11バイトだけ。上限8機なら問題にならない。

計測環境の注意: この計測はGPUの無い環境(SwiftShaderによるソフトウェア描画)で行ったため、
フレーム時間そのものは実機の値ではない。上表のdrawCall数・三角形数・テクスチャ数・
CPU時間はGPUに依存しない値なので、そのまま参考にできる。

### マス目で作った柄の共有方法(2026-08-11)

エディタで書き写した展開図をどう配るか。**プリセット化とコード化の両方**を用意した。

#### 道1: プリセットにする(実機の柄向け)

`makeGridPattern(id, 列数, マス目)` を用意し、`PATTERNS` へ1行で足せるようにした。
v1形式はプリセット番号に5bit=32枠を確保済みで、現在4枠しか使っていない(残り28枠)。
5つ目以降を足すと自動的にv1へ切り替わり、**コードは7文字前後のまま**。
手元が知らない番号を受け取ったクライアントは交互(alt)へフォールバックする(色は正しく読める)。

#### 道2: マス目をそのままコードにする(v2形式・デプロイ不要)

先頭`H`の新形式。v0(16進6桁)・v1(`G`始まり)は一切変更していない。

    [3bit 列数] [5bit 段数-1] [6bit 横周期] [1bit 塗り潰し] [2bit テープ]
    [4bit 色数-1] [4bit×色数 パレット] [横周期×段数×1マスのbit数 マス目]

**横方向の繰り返しを自動検出して1周期ぶんだけ載せる**のが効く。実機の柄は
「一周に山が3回」のような周期を持つことが多く、MV-56b(24列×13段・4色)は
8列周期なので312マスが104マスに畳まれる。

| 内容 | 文字数 |
|---|---|
| MV-56b(24列×13段・4色、8列周期) | **50** |
| 繰り返しの無い24列×13段・4色 | 134 |
| 48列×24段・4色 | 470 |
| 48列×24段・16色 | 940(上限超過 → プリセットの道へ) |

上限は512文字。手打ちはできないがコピペ・URL・QRなら通る。超える大きさのマス目は
コードにできないので、その場合はエディタが警告を出してプリセットの道を案内する。

#### 帯域への影響(wrangler devで8機を実測)

| メッセージ | 現行6桁 | v2・50桁 |
|---|---|---|
| `snap`(4Hz常時) | 286 B/通 | **292 B/通(実質不変)** |
| `roster`(局面変化時のみ) | 861 B/通 | 1141 B/通(+280 B = 1人あたり+35 B) |

appearanceは`snap`に入らないので、飛行中ずっと流れるストリームは変わらない。
30分フライトなら`snap`が約2.1 MBに対し`roster`側の増分は十数KBで、全体の1%未満。

#### 変更点

- `balloon-appearance.js`: `makeGridPattern()` / `GRID_PATTERN` / v2の符号化・復号 /
  `MAX_CODE_LENGTH` 24→512
- `server/src/room-core.js`: appearanceの受け入れを `/^[0-9A-Za-z]{1,512}$/` に
  (中身を解釈しない不透明な文字列として扱う方針は変えず、長さと文字種だけ)
- `main.js`: 下書き→見た目の変換でマス目を引き継ぐ(当初これが抜けていて、
  貼り付けたコードが往復しない不具合があった)。展開図から取り込んだ柄を選んでいる
  ことを示す注記も追加(柄ボタンには並ばないため)
- `index.html`: コード入力欄の maxlength 24→512
- エディタ: 手描きのマス目からv2コードを出力し、周期による短縮と長さを表示。
  実装へ足す下書きも `makeGridPattern` を使う短い形に

### 三角パネルへの対応(第4段階、2026-08-12)

Ultramagic MV-65(クライアントNHS向け機)の実機平面図を見ると、□のマス目ではなく
◣(対角線で2色に割った三角)のパネルを効果的に使っている柄だった。v2(マス目)は
1マス1色が前提のため、この柄は表現できなかった。

#### 決めたこと

- **v0/v1/v2の互換は一切変更しない**。新形式は先頭`J`のv3として追加しただけで、
  既存コードの読み書きに関わるコードパスには一切触れていない
- **マス目1個を対角線で2色に割る**方式にした。マス目ごとに色A・色B・分割方向
  (`\`または`/`)の3値を持つ。v2の「マス目=1色」を「マス目=2色+方向」に拡張した形で、
  データ構造・エディタの操作感をv2からそのまま流用できる
- 符号化はv2と同じ骨格(列数・段数・横周期・テープ・パレット+マス目の生ビット列)に
  マス目1個あたりのビット数を「色A(bpc)+色B(bpc)+方向(1bit)」に変えただけ。
  横周期の自動検出(`horizontalPeriodTri`)もv2の考え方をそのまま踏襲

#### 見つかった描画上の制約と修正

既存の`buildGoreTexture()`(球皮テクスチャの生成)は、1マスの横幅ぶんを**1回だけ
サンプリングして単色で塗る**実装だった。v0〜v2はどのマスも中で色が変わらないので
これで問題なかったが、v3は対角線でマスの中で色が変わるため、この実装のままだと
斜めの境界線が描けず、マス全体が一方の色でベタ塗りされてしまう。

これはv3固有の新しい要求で、既存パターンの動作には一切影響しない変更で対応した。
`resolvePattern()`が返す形に`subCell`という真偽値を追加し(v3だけtrue、他は未設定
=false相当)、`buildGoreTexture()`側は`subCell`がtrueの柄だけ**512px全幅を1pxごとに
連続サンプリング**する経路に分岐させた。他の柄が通る従来の粗いサンプリング経路は
1行も変更していない。

#### 検証

- `node --test`: 既存56件(v0〜v2)が無傷であることに加え、v3の新規テスト
  (dir=0/dir=1の幾何・往復・周期圧縮・色数上限・不正マス目の丸め・
  `resolvePattern`の形一致)を追加し、計57件pass
- 「v0/v1/v2はv3を足しても一切変わらない」ことを固定するテストを追加
- ブラウザ: エディタに「三角パネルを塗る」モードを追加し、マス目モードの柄を
  種にして三角へ変換→対角線で塗り分け→3Dプレビューに反映、を確認。
  `subCell`の修正前は3Dプレビュー上で斜め境界が出ずマス全体が単色になる不具合が
  実際に再現し、修正後は展開図・3Dプレビューの両方で斜めの境目が正しく出ることを確認

#### 変更点

- `balloon-appearance.js`: `makeTriGridPattern()` / `TRI_GRID_PATTERN` /
  v3の符号化・復号(`V3_PREFIX = 'J'`) / `resolvePattern()`に`subCell`フラグ追加
- `main.js`: `buildGoreTexture()`に`subCell`時の全幅連続サンプリング経路を追加
  (既存経路は変更なし)。柄下書きへの`triGrid`引き継ぎ、三角パネル注記の追加
- `index.html`: `#balloon-trigridnote`の追加
- `i18n.js`: `balloon.triGridNote`を7言語に追加
- エディタ: 「三角パネルを塗る」モードを追加。マス目からの一括変換、塗り/反転
  ツール、方向キーでのスロット・色切り替えに対応

#### 残っていること

- `balloon-supporters`(`poland/`)への展開は別途(第1〜3段階と同じ流れで移植する)

#### v4: 5色以上・段数が多い柄で512文字を超える問題への対応(2026-08-12)

実機の平面図(7色・24列×15段)をそのまま書き写したところ、コードが**515文字**になり
上限512文字を超えて共有できない不具合が見つかった。

##### 原因

v3は1マスにつき固定で `[a bpc bit] + [b bpc bit] + [dir 1bit]` を payload に持つ。
色数が5〜8色になるとbpc(1色あたりのbit数)が2→3に増え、1マスの単価が5bitから
7bitへ跳ね上がる。実機の柄には「対角線で塗り分けた場所」より
**「1色でベタ塗りの場所」(裾のノイズ・単色の帯など)のほうが多い**ことが珍しくないが、
v3はそのようなマスでも同じ7bitを払わされていた(該当データは360マス中114マスが
ベタ塗り=a===bで、dirの値は本来無意味)。

##### 対応: v4(先頭`K`)を追加。v3(`J`)は一切変更しない

マスごとに1bitの旗を先頭に立て、ベタ塗りのマスは色1つぶんだけで済ませる形式を追加した。

```
[1bit 旗=1(ベタ塗り)] + [bpc bit 色]                        … 旗+bpc bit
[1bit 旗=0(対角線あり)] + [bpc bit a] + [bpc bit b] + [1bit dir] … 旗+2bpc+1 bit
```

ベタ塗りが多い柄ほど短くなる。逆に対角線だらけの柄では旗ビットの分だけv3より
1bit/マス長くなるため、`encodeAppearance()`は**v3・v4の両方を作って短い方を採用する**
(v0が現行の見た目ならv0のまま出す、という第1段階からの一貫した方針と同じ)。

- 実機データ(7色・24×15段、ベタ塗り114/360マス)で **515文字 → 496文字** に短縮され、
  上限内に収まった
- v3(`J`)の符号化・復号は一切変更していない。既に共有されているv3コードはそのまま読める
- 塗り分け(見た目)が一致することを`colorSlotAt()`で1,440点サンプルして確認(0件不一致)。
  JSON生データ上の往復不一致(ベタ塗りマスの`dir`値は保存されず`0`に正規化される)は
  見た目に影響しないので許容している
- `node --test`: 既存57件(v0〜v3)が無傷であることに加え、v4の新規テスト5件
  (ベタ塗り混在でv4が選ばれる/対角線だらけでv3のままになる/実機相当データが上限内に
  収まる/壊れたコードのフォールバック/v0〜v3の非破壊)を追加し、計62件pass

##### 残っていること

- `balloon-supporters`(`poland/`)・柄エディタへの反映は別途

### 残っていること

- 実機の柄を数式で表す方針は取りやめ。エディタで数式へのフィッティングを試したところ、
  サファイア柄の一致率は5割程度にとどまった。実機の柄は写真合わせの例外を含むため、
  **マス目をそのまま書き写してプリセットとして持たせる**方針とする。
  数式(カーネル)のほうは、プレイヤーが自分で柄を作るための道具として残す
- 柄エディタは作成済み(実機と同じ球皮形状の3Dプレビュー+メーカー図面と同じ列×段の
  マス目。図面画像は重ねずに独立した窓で見比べる方式)。マス目からPATTERNSへの
  下書きコードを書き出せるところまで。実際のプリセット追加は未着手
- `balloon-supporters`(`poland/`)への展開は、形式が固まってから別途

### 気球柄カスタマイズ、公開版の対応状況を再確認・再入室時の柄更新漏れを修正(2026-08-14)

「公開版にも気球柄の対応を拡張したい」という要望を受け、`balloon-supporters`(`poland/`)の
現状(`origin/main`)と本リポジトリの`prototype/`・`mobile/`を全項目突き合わせて確認した。

**確認結果**: 上記の「残っていること」に書かれていた「poland/への展開は別途」「柄エディタへの
反映は別途」は、その後の別コミット(`23c1c9f`〜`37399ff`、いずれもマージ済み)で**既に完了して
いた**。`balloon-appearance.js`(v0〜v4の符号化・復号、16色対応)・`net.js`の`BALLOON_COLORS`・
`main.js`の柄モーダル一式(数式/カーネル・展開図マス目・三角パネル塗り)・`index.html`の
`#balloon-modal`マークアップ・`i18n.js`の`balloon.*`キー(7言語×40キー)は、`poland/`・
`prototype/`・`mobile/`の3箇所で**完全に一致**していることを確認した(クライアント側の
機能・見た目に差は無い)。

**唯一見つかった実質的な差分(サーバー側)**: `server/src/room-core.js`の`join()`のうち、
フライト中/リザルト中に同名プレイヤーが再接続する「復帰(reattach)」の経路。`poland`側は
復帰時に新しく送られてきた`color`/`appearance`でゴースト席の値を更新するが、本リポジトリの
`server/src/room-core.js`にはこの更新処理が無く、**復帰したプレイヤーの気球の色・柄が
初回参加時点のまま固定され、フライト中に選び直しても他プレイヤーへ反映されない**という
不具合があった(スマホでアプリを切り替えてWebSocketが切れ、再接続する場面などで発生しうる)。

`poland`の実装と同じロジックを移植して修正:

```js
ghost.connected = true;
ghost.disconnectedAt = null;
ghost.lastSeen = now;
// 再接続時に送られてきた最新のcolor/appearanceも反映する(反映しないと、初回参加時点の
// 見た目に固定されたまま、後から選び直した柄がずっと他の全員へ配られなくなる)
if (Number.isInteger(color) && color >= 0 && color < 16) ghost.color = color;
if (typeof appearance === 'string' && /^[0-9A-Za-z]{1,512}$/.test(appearance)) {
  ghost.appearance = appearance.toUpperCase();
}
```

`server/src/room-do.js`は元々`join()`呼び出し時に毎回(初回・再接続とも)URLの`color`/
`appearance`パラメータをそのまま渡していたため、こちらの変更は不要だった。

テストも`poland`から該当分を移植(不正な値では上書きしないことも確認):
`'飛行中の再入室では、新しく送られてきたcolor/appearanceで更新される(選び直した柄が
固定されないように)'`。

- 検証: `node --check server/src/room-core.js`・`node --test server/test/*.test.mjs`
  (33件pass、既存回帰+新規1件)・`node --test prototype/test/*.test.mjs`
  (62件pass、クライアント側は無変更のため既存回帰のみ)

## 決定事項

- **ゲーム内容**: 実在の日本の地形を3D化した空間で熱気球を操縦するフライトゲーム
- **対象エリア**: ゲーム開始前にプレイヤーが地域を選択できる(日本全国から自由に選択)
  - → 地理院タイルを動的に取得する仕組みが必要
- **ゲームの目的**: 熱気球競技の競技規定に基づく **JDG(Judge Declared Goal)競技** を行う
  - 競技委員(ゲーム側)が設定したゴール(ターゲット)にマーカーを投下し、ターゲット中心からの距離を競う
- **風のモデル**: 高度ごとに風向・風速を設定するゲーム的レイヤーモデル
  - 低〜中高度: **パイバル(パイロットバルーン)観測データを入力**して風向・風速を定義する
  - 高高度: 天気図(気圧配置)の影響を受けた風として設定
- **リアリティの方向性**: 中間(操作は簡単、浮力の慣性や着陸・投下判定はそれなりにリアル)
- **競技タスクの範囲**: まずJDG単体で完成させる。ただしタスク定義はデータ化しておき、将来PDG・FON・HWZ等を追加しやすい設計にする
- **離陸地点**: タスク設定で「個別離陸(プレイヤーがエリア内で自由に選択)」「共通ローンチサイト固定」の両方を指定できるようにする(実競技に近い形)
- **マーカー投下**: 物理落下を簡易再現。重力+空気抵抗で落下し、落下中も風に流される(低高度から投げるほど正確、という実競技の駆け引きを再現)
- **パイバルデータ入力**: 高度ごとの風向・風速をテーブルで手入力できる+典型的な風パターンのプリセットも用意
- **入力単位**: 実競技慣行に合わせる(高度はフィート刻み〔例: 500ft〕、風向は磁方位度数、風速はノット)。表示単位は設定でm/s系に切替可
- **スコアリング**: マーカー着地点からターゲット中心までの距離(m)をそのままリザルトとする。マーカー未投下時は着陸地点で計測
- **飛行中の計器・情報表示**: 以下をすべて表示する
  - 基本計器(高度計・昇降計〔バリオメーター〕)
  - 地理院地図+自機位置・軌跡・ターゲット表示(GPS相当)
  - 現在高度の風向・風速
  - ゴール(ターゲット)の位置
  - 入力済みパイバル表の参照
- **フライト前フェーズ**: 実競技のブリーフィングを再現する
  - タスクシート提示(ターゲット座標・離陸条件・制限時間)→ パイバルデータ確認/入力 → 地図で離陸地点選択 → 離陸
- **時間の扱い**: 基本は実時間スケール+プレイヤーが任意に×2〜×8の時間加速を使える(風読みのリアリティを保ちつつ1プレイを短縮)
- **視点**: ゴンドラからの一人称視点が基本、俯瞰確認用に三人称・真下視点へ切替可
- **競技相手**: まずはソロのスコアアタック。同一タスク・同一風での自己ベストや過去記録との比較機能を持たせる
- **制限時間・終了条件**: タスクシートで制限時間(スコアリングピリオド)を指定。時間内にマーカー投下できなければ着陸地点計測にフォールバック
- **操作方式**: いずれも長押し操作
  - **バーナー**: 長押しで噴射(加熱の効きは数秒遅れて現れる)
  - **排気**: リップラインを長押しで引き、気球頂部のパラシュート(リップパネル)が開いて排気する方式(ベント方式ではない)
- **燃料**: プロパン残量表示あり。消費はバーナー噴射時間に比例するシンプルなモデル(残量が実質の飛行可能時間になる)
- **記録保存・共有**: 自己ベストはブラウザのlocalStorageに保存。風+タスクの条件はURLパラメータ(シード)にエンコードし、URL共有だけで同条件に挑戦できる(サーバー不要)
- **対象エリアの広さ**: 1タスクあたり15〜20km四方(実競技の飛行エリアに近いスケール。DEM10B+必要に応じ高精度化)
- **プラットフォーム**: ブラウザ+Three.js に決定
  - 地理院タイルの動的取得と最も相性が良い(CORS対応済み・fetchでそのまま取得可)
  - 「URLを開くだけで同条件(風+タスク)に挑める」配布・共有性がソロ+記録比較の設計と噛み合う
  - 将来Electronでデスクトップ版に包む移行も容易

## 使用予定の国土地理院データ(整理済み)

- **標高タイル(DEM)**: PNG標高タイル。DEM5A(5mメッシュ・高精度)/ DEM10B(10mメッシュ・全国カバー)。地形メッシュ生成の核
- **地理院タイル(画像)**: 標準地図・淡色地図・航空写真・陰影起伏図など。地形テクスチャに使用
- **ベクトルタイル**: 道路・河川・建物輪郭など(必要に応じて)
- 利用規約: 出典明記(「国土地理院タイル」等)で商用含め利用可

## 熱気球のゲーム性(前提の共有)

気球は「バーナーで上昇・冷却/排気で下降、水平移動は風任せ」という乗り物。
→ **高度によって風向・風速が変わる中を、上下操作だけで目的地を目指す**というゲーム性が自然に成立する。

## 未決定事項(次に決めること)

1. **技術詳細設計**(仕様は固まったので、次は実装前の技術設計フェーズ)
   - キー/マウスの具体的割当(バーナー・リップライン・視点切替・時間加速)
   - DEMズームレベル、タイル取得・キャッシュ戦略、地形メッシュのLOD
   - 風シードのURLエンコード形式
   - 画面遷移(エリア選択 → ブリーフィング → フライト → リザルト)のワイヤーフレーム
